import React from 'react'
import { Grid } from '../elements'

const MessageList = (props) => {
    return (
        <React.Fragment>
            <Grid>

            </Grid>
        </React.Fragment>
    )
}

export default MessageList
